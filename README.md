# chrome-extension

This chrome extension add a copy and paste ability to services in the AWS Console. This helps with deleting that service by coping the word 'delete' or 'permanetly delete' on select pages.

Currently configured AWS services:
- S3 Bucket Delete Object
- S3 Bucket Delete
- IAM Role Delete
