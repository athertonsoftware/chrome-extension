{
  "manifest_version": 3,
  "name": "Copy To Clipboard",
  "version": "1.0.2",
  "description": "When you have to copy a word or phrase to delete an object or service. This extension makes certain text clickable to copy to clipboard on AWS console pages.",
  "permissions": ["clipboardWrite"],
  "content_scripts": [
    {
      "matches": [
        "https://*.console.aws.amazon.com/*",
        "https://*.aws.amazon.com/*"
      ],
      "js": ["content.js"],
      "css": ["style.css"],
      "run_at": "document_idle"
    }
  ]
}
